node_modules/
package-lock.json
# Raw movements never belong in git — they go to object storage. Git history is
# permanent, so anything committed here can never truly be removed.
data/runtimes/
data/raw/
data/platforms/
